server:
  #port: 443
  port: 8080
  #ssl:
    #key-store: classpath:ssl/keystore.p12
    #key-store-type: PKCS12
    #key-store-password: rlaxoghks07

spring:
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 25MB

  #jpa:
    #properties:
      #hibernate:
        #default_batch_fetch_size: 1000
        #format_sql: true
        #show_sql=true: true
    #hibernate:
      #ddl-auto: create
    #show-sql: true


  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: rlaxoghks07^T
    url: jdbc:mysql://localhost:3306/login?characterEncoding=utf8&useUnicode=true&mysqlEncoding=utf8&zeroDateTimeBehavior=convertToNull&serverTimezone=Asia/Seoul

# jwt.secret.key의 값은 256bit 이상이어야 합니다.
jwt:
  secret:
    key: x!A%D*G-KaPdSgVkYp3s5v8y/B?E(H+M